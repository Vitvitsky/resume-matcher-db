# PostgreSQL connection settings
POSTGRES_DB=resume_matcher
POSTGRES_USER=resume_matcher
POSTGRES_PASSWORD=change_me_strong_password
POSTGRES_PORT=5432
POSTGRES_HOST=localhost

# Full DATABASE_URL (используется в Alembic и resume-matcher-engine)
DATABASE_URL=postgresql+psycopg2://resume_matcher:change_me_strong_password@localhost:5432/resume_matcher
