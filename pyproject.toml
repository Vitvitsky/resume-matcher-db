[project]
name = "resume-matcher-db"
version = "0.1.0"
description = "PostgreSQL сервис хранения для resume-matcher (миграции Alembic)"
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.14.0",
    "psycopg2-binary>=2.9.0",
    "sqlalchemy>=2.0.0",
    "python-dotenv>=1.2.1",
]
